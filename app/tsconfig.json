{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    "jsx": "react-jsx",       // or "react" if React 17 or earlier
    "lib": ["DOM", "ESNext"], // needed to type SVG/HTML elements
    "module": "ESNext",
    "target": "ESNext"
  }
}
